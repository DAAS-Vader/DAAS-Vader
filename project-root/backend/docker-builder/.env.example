# Docker Builder Service Environment Configuration

# Service Configuration
NODE_ENV=development
PORT=3001

# Build Configuration
DOCKER_BUILD_DIR=/tmp/docker-builds
MAX_CONCURRENT_BUILDS=3
BUILD_TIMEOUT=600

# Walrus Storage Configuration
WALRUS_PUBLISHER=https://publisher.walrus-testnet.walrus.space
WALRUS_AGGREGATOR=https://aggregator.walrus-testnet.walrus.space

# Docker Builder Endpoint (for integration)
DOCKER_BUILDER_ENDPOINT=http://localhost:3001

# Registry Configuration (optional - for testing)
REGISTRY_URL=localhost:5000
REGISTRY_USERNAME=testuser
REGISTRY_PASSWORD=testpass

# Nautilus Integration (when deployed in Nautilus)
NAUTILUS_ENDPOINT=http://localhost:8080